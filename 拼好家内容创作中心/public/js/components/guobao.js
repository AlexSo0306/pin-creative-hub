/* ============================================================
 * 锅宝 · 角色定义（Mood Mates 引擎的角色数据包）
 *
 * ⚠ 2026-09-24 改版：外观按原项目（grok-icon-study）**1:1 对齐**，不再是「珐琅蓝小锅」。
 *    依据是原项目自己的几何数据 + 它自己渲染出来的图，不是肉眼印象。
 *    量法见 tools/mascot-verify/README.md；脚本见 /tmp/grok-ref/*.mjs。
 *
 * ── 原版实测（两路独立取证，互为对照）─────────────────────────────
 *   ① 几何数据  GROK_GEO（从 Grok Bot.app v0.18.0 的 app.asar 抽出的原始坐标）
 *   ② 像素测量  replica 页面 #disk 放大 4× 截图 → 洪水填充找圆 + PCA 求眼条主轴
 *
 *   原版 idle 身体   : 228.54 × 228.46 → **正圆**（宽高比 1.0004；
 *                      像素复核 1.0000~1.0049，圆度 0.968）
 *   原版 idle 眼睛   : 眼形 idx 0 = **45.2 × 21.1**（长宽比 2.14，钝头圆角条）
 *                      眼形 idx 8 = 51.5 × 27.6
 *                      倾角 63° 是**姿态**（POSE.turn/tilt/roll）造成的，不是眼形本身
 *   原版配色「蓝」   : palette.blue = #2A92FE（深色档 #0E74E0）
 *   原版**没有**     : 高光 / 釉面 / 体积渐变 / 地面投影 / 嘴 / 腮红 / 眉毛 / 瞳孔
 *                      —— 全身只有「一枚纯色圆 + 两条白色竖条」
 *
 * ── 25 个眼形的形态普查（这决定了「表情」该怎么配）────────────────
 *   对 GROK_GEO.eyes 的 25 个眼形逐只做：面积 ÷ 凸包面积、下缘中段是否上凹。
 *
 *     面积/凸包    全部落在 0.965 ~ 1.000   → **全是凸形**（胶囊 / 圆角条 / 圆）
 *     下缘上凹     全部为负               → 下缘永远外凸
 *     ⇒ **原版根本没有「∩ 拱形笑眼」**。拱形是上游引擎自己的语汇，不是原项目的。
 *
 *   原版 25 个眼形的构成：
 *     竖条 15 个 · 近圆/近方 5 个 · 横条 3 个 · 偏扁 2 个
 *
 *   原版怎么表达情绪（EYE_PLAYLIST，见 replica/src/tables.js）：
 *     开心 happy    = [2, 11, 17, 19]  → **更大更长的竖条**（idx 19 = 31.7×60.1）
 *     惊讶 surprised= [3, 21]          → **近圆**（idx 3 = 50.5×56.1）
 *     闭眼 sleeping = [13, 22, 4]      → **横条**（idx 13 = 52.5×20.8）
 *     困倦 drowsy   = [4, 22, 13]      → 横条
 *     扫读 searching= [15, 9, 3, 20, 12, 18] → 竖条与近圆混合
 *
 *   ⇒ 所以本文件把上游那几个「拱形 / 弯曲」槽位**同名覆盖**成原版的凸形语汇。
 *     覆盖机制：engine.js:94-100 先建上游眼环族，再把 raw.eyeShapes 逐项**赋进同一张
 *     family 表**，因此同名即覆盖（不是新增）。
 *
 * ── 一个引擎能力差异，以及我们对它打的那一刀 ──────────────────────
 *   原版的**双眼间距随眼形变化**（dx 19.6~35.8），它靠这个把「内缘间隙」稳住：
 *     横条 idx 4/13/22：w 50~56，dx 31~32 → 间隙  5.2~13.0
 *     近圆 idx 3/21  ：w 44~46，dx 35~36 → 间隙 25.4~25.7
 *     竖条 idx 0/8   ：w 21~28，dx 24~27 → 间隙 27.7~36.0
 *   上游引擎却把 dx 定死在**角色级**，于是眼形一宽双眼就叠在一起。
 *   → 本地补丁 ④（geometry.js buildCustomEyePair）：让形状自带 dx。
 *   这一刀是必要的，不是顺手改的 —— 不补它，横条就只能是 37 宽（间隙 11），
 *   而原版是 50~56 宽，长轴差 27%~33%，肉眼一看就知道不对。
 *
 * 剪影：puff 谐波生成器，waves 传空数组 = 无扰动 = **正圆**（geometry.js:83）。
 *   ⚠ 出画检查：r × 104 × (1 + 最大隆起) ≤ 118
 *     1.10 × 104 × (1 + 0) = 114.4  ✓
 *
 * 配色：品牌主色 #0099ff（Alex 指定），7 个 states **全部同色** ——
 *   原项目登录页的角色是固定色（`color: "black"`），情绪不靠变色表达。
 *
 * 扁平化：palette.flat 是**我们对引擎打的另一处补丁**（render.js 三处），
 *   用来关掉上游恒开的「伪 3D 体积渐变 + 底部 AO + 地面投影」。
 *   四处补丁的说明与 diff 见 vendor/mood-mates/README.md。
 *
 * 加载顺序：必须在 vendor/mood-mates/core/engine.js 之后。
 * ============================================================ */
window.MoodMates.characters.register({
  id: 'guobao',
  name: '锅宝',
  en: { name: 'Guobao', desc: 'A flat #0099ff circle with two white vertical-bar eyes' },
  industry: 'general',
  desc: '扁平纯色圆 + 两条白色竖条眼，32 种表情全靠眼形变化',

  /* 1. 身体剪影 —— waves 为空 = 正圆 */
  body: {
    type: 'puff',
    r: 1.10,
    waves: []          // 不要任何扰动：Alex 要的就是「圆形」
  },

  /* 2. 面部拟合 —— 正圆居中，五官不再需要偏移 */
  face: { x: 0, y: 0, sx: 1, sy: 1, eye: 1 },

  /* 3. 色板 */
  palette: {
    body: '#0099FF',
    eye: '#FFFFFF',      // 白眼睛（原版是白条）
    zzz: '#FFFFFF',      // 睡眠 Z 也用白，免得在纯色圆旁边冒出一个彩色异物
    flat: true,          // ★ 本地补丁：四处渐变 stop 同色 → 纯色扁平
    gloss: 0,            // 不要釉面高光
    /* 情绪不改色 —— 原版登录页角色是固定色。全档同色，避免任何一档漏填回退。 */
    states: {
      base: '#0099FF',
      dim: '#0099FF',
      soft: '#0099FF',
      blush: '#0099FF',
      angry: '#0099FF',
      alert: '#0099FF',
      off: '#0099FF'
    }
  },

  /* 4. 眼型基调 —— 关键：**h > w** 就是竖条。
     引擎的 lens 把「长度」放在 X 轴（w）、「厚度」放在 Y 轴（h），
     所以让 h 远大于 w，得到的就是一条竖条，**不需要 tilt 旋转**
     —— 这点很重要：一旦用 tilt 把眼睛转 90°，引擎里 wide / listen / scan
     这几个强制 `tilt: 0` 的槽位就会把竖条转回横条，表情语义全反。
     量出来的目标：原版 idle idx 0 = 45.2 长 × 21.1 厚，占身直径 0.198 / 0.092，
     故 w=21 h=45。
     dx 24 = 原版 idx 0 的 24.4，双眼内缘间隙 27 ≈ 原版 19.7（略宽，读起来更透气）。
     taper 0.14：**不是猜的，是拟合出来的**（脚本 /tmp/grok-ref/fit-taper.mjs）。
       把原版眼形与我们的 lens 归一到同一个「长×厚」包围盒，比 10 档厚度剖面：
         idx 0      原版 0.78 0.95 0.99 0.99 0.99 0.99 1.00 0.99 0.96 0.78
         taper 0.14 锅宝 0.80 0.93 0.95 0.99 1.00 1.00 0.99 0.95 0.93 0.80  ← 剖面误差 2.0%
       扫过 0.02~0.60 全区间，0.14 是唯一最优（次佳 0.16）。
       ⚠ 上一版写 0.3，是按「20%~80% 处宽度 75%」口算的 —— 口算与实测差了整整一倍。
         原版的端部比看起来**钝得多**（末档只掉到 0.78，几乎是个圆角矩形）。 */
  eyeStyle: {
    dx: 24,            // 两眼中距 48 ≈ 原版 idle 的 48.8
    cy: 112,           // 略高于圆心，读起来才像脸
    w: 21,             // 横向宽度
    h: 45,             // 纵向长度 → 竖条
    taper: 0.14,       // 钝头圆角（由原版剖面拟合，见上）
    tilt: 0,           // 纯竖线
    bend: 0
    /* ⚠ 刻意不写 pupil：没有 pupil 时引擎走「bean 豆眼」分支
       （render.js:286），每只眼就是**一条 path 填 palette.eye** ——
       这正是原版的做法（两条 <path fill="#fff">）。
       写了 pupil 反而会变成「眼白 + 虹膜 + 瞳孔 + 高光」的分层眼球。 */
  },

  /* 4b. 专属轮廓 —— 覆盖上游槽位，全部换成原版的**凸形**语汇
   *
   * 为什么必须覆盖：上游 `happy`/`happy2` 是 ∩ 拱（bend 0.62 / 0.5），
   * 而 bend 在 lens 里是 `(o.bend||0) * h` —— 它按**眼高**缩放。
   * 我们的基准眼高是 45（竖条），远大于上游假定的横扁眼，
   * 于是拱的位移量被放大到超过条带半厚，上下缘交叉，退化成一个**三角形 ▲**。
   * 实测受害表情：10 开心 / 31 接收任务 / 33 任务完成。
   *
   * 而原版 25 个眼形**没有一个拱形**（面积/凸包 0.965~1.000）。
   * 所以正确的修法不是「把 bend 调小」，而是**换成原版真的有的形态**。
   *
   * ── 参数怎么来的：不是调出来的，是**换算**出来的 ──────────────────
   *   原版眼形是倾斜的，bbox 会被倾角撑大（idle idx 0 的 bbox 是 31.4×42.0，
   *   真实长宽却是 45.2×21.1）。所以一律取 **PCA 真实长短轴**：
   *     横向为长的（横条）  → w = PCA 长轴，h = PCA 短轴
   *     纵向为长的（竖条/圆）→ w = PCA 短轴，h = PCA 长轴
   *   我们 tilt = 0（Alex 要竖线），所以 PCA 长轴就是屏幕上的竖直方向，
   *   两者一一对应，不需要再折算。
   *   原版身体直径 228.541，锅宝 228.8 → k = 1.00113，几乎 1，直接乘。
   *
   *   下表每一行都对应原版一个具体眼形 idx（EYE_PLAYLIST 里查得到用途）：
   *     槽位      w×h        taper  dx     ← 原版 idx（用途）
   *     happy    22.8×61.3  0.14   28.9   ← 19  （EYE_PLAYLIST.happy）
   *     happy2   20.6×56.4  0.12   28.3   ← 17  （happy / playful / celebrate）
   *     delight  39.0×88.3  0.12   33.4   ← 2   （celebrate 里最大的那条）
   *     intent   18.6×62.0  0.14   25.9   ← 16  （working —— 干活时那条细长的）
   *     listen   25.0×60.5  0.14   29.2   ← 1/10（listening —— 注意听）
   *     listen2  22.0×55.0  0.14   28.5   ← 1/10 的窄一档
   *     wide     46.0×56.5  0.36   35.8   ← 21  （surprised / scared）
   *     wide2    43.6×56.5  0.36   34.5   ← 3   （curious / surprised）
   *     closed   50.4×14.1  0.05   33.7   ← 13  （sleeping / drowsy / bored）  dx 已 +2
   *     closed2  54.8×14.0  0.06   34.1   ← 22  （sleeping / drowsy / sad）    dx 已 +2
   *     sleepy   56.5×13.8  0.05   32.8   ← 4   （drowsy / bored / sad）       dx 已 +2
   *
   *   ⚠ scan / scan2 / scan3 **不在本表里 —— 故意用上游原值**。
   *     上游在这三格给的是扁眼（27.3×20.7 / 24.8×25.2 / 28.6×17.1，长/短 ≈ 1.2~1.7），
   *     恰好对上原版 searching 的近圆语汇（idx 9/12/18 长/短 = 1.09/1.09/1.03）。
   *     上一版曾把它们改成 50.4×14.1 的粗横条，结果池子里带 scan 的情绪
   *     （19 满意 / 39 输出回复 / 40 检索资料 / 03 好奇 / 30 思考中）全变成闭眼。
   *     详见下方 eyeShapes 里那段纠正记录。
   *
   * ⚠ 横条族 dx 统一 +2（唯一一处刻意偏离原版量值）：
   *   原版横条的内缘间隙最小只有 5.2（idx 4），因为它的端部是圆头，视觉上还分得开；
   *   我们的 lens 端部收尖，同样的间隙会糊成一条 —— 实测 06 休眠 / 15 疲惫 在
   *   接触表里就并成一根长条，读起来像坏了。+2 之后最小间隙 9.1，两眼分得清。
   *   这是表达力差异带来的必要补偿，不是随手改的数。
   *
   * ⚠ intent / listen 也要覆盖：上游这两个槽位是按「圆眼角色」定的比例，
   *   在我们 21×45 的竖条基调下会算出 16×53 / 15×49 —— 比原版的 listening
   *   （idx 1/10 实测 25×60）细一大截，看着像没睡醒。按原版量值覆盖。
   *
   * ⚠ dx 是**每个形状自带**的，靠本地补丁 ④（geometry.js buildCustomEyePair）。
   *   上游把 dx 定死在角色级，眼形一宽双眼就叠在一起；原版恰恰是按形状给 dx
   *   （19.6~35.8），把双眼内缘间隙稳在 5~36px。不补这一刀就没法 1:1。
   *   本表 dx 直接取原版量得的数，换算后间隙落在 5.2~36.0，与原版同量级。
   *
   * taper：同样**逐个拟合**，不是估的。原版端部比直觉钝得多：
   *   竖条族（idx 0/8/19/17/2）剖面误差最优解 0.10~0.14 → 取 **0.14 / 0.12**
   *   横条族（idx 4/13/22）几乎就是**圆角矩形**（末档只掉到 0.88~0.92）→ 取 **0.05**
   *   近圆族（idx 21/3）端部收得更明显（末档 0.60）→ 取 **0.36**
   *   三族剖面误差都落在 1.1%~2.2%，即 lens 已能很好表达原版轮廓。
   * ⚠ 但 lens 的上下缘在两端汇合成**一个点**，原版是圆头 —— 这是表达力上限，
   *   只能逼近。taper 越小越接近矩形，代价是端点那个「收尖」越突兀。
   *   取到 0.05 时剖面误差 1.1%，已经比 0.14 那档更接近原版。 */
  eyeShapes: {
    /* ── 开心：原版 happy = idx [2,11,17,19]，是**更长、粗细几乎不变**的竖条 ── */
    happy:  { w: 22.8, h: 61.3, taper: 0.14, dx: 28.9 },   // idx 19
    happy2: { w: 20.6, h: 56.4, taper: 0.12, dx: 28.3 },   // idx 17

    /* ── 任务完成（33 专属）：原版 celebrate 里最大的那条 ── */
    delight: { w: 39.0, h: 88.3, taper: 0.12, dx: 33.4 },  // idx 2

    /* ── 专注（16 专属）：原版 working = idx [7,16,11,10]，取 idx 16 那条细长的 ── */
    intent: { w: 18.6, h: 62.0, taper: 0.14, dx: 25.9 },   // idx 16

    /* ── 聆听 / 等待输入：原版 listening = idx [10,1,19]，是**粗一点的竖条** ── */
    listen:  { w: 25.0, h: 60.5, taper: 0.14, dx: 29.2 },  // idx 1 / 10
    listen2: { w: 22.0, h: 55.0, taper: 0.14, dx: 28.5 },  // 窄一档

    /* ── 惊讶 / 慌张 / 出错：原版 surprised & scared = idx [3,21]，是**近圆** ── */
    wide:  { w: 46.0, h: 56.5, taper: 0.36, dx: 35.8 },    // idx 21
    wide2: { w: 43.6, h: 56.5, taper: 0.36, dx: 34.5 },    // idx 3

    /* ── 闭眼 / 困倦：原版 sleeping & drowsy = idx [13,22,4]，是**横条** ──
     * 上游原本是 h×0.12 = 5.4（极扁小点），原版横条实测 ~50×14，差得很远。 */
    closed:  { w: 50.4, h: 14.1, taper: 0.05, dx: 33.7 },  // idx 13
    closed2: { w: 54.8, h: 14.0, taper: 0.06, dx: 34.1 },  // idx 22
    sleepy:  { w: 56.5, h: 13.8, taper: 0.05, dx: 32.8 },  // idx 4

    /* ── 扫读：**故意不覆盖**，用上游原值 ──
     * 上游 scan/scan2/scan3 = { w×1.3 / 1.18 / 1.36, h×0.46 / 0.56 / 0.38 }，
     * 在我们 21×45 基调下是 27.3×20.7 / 24.8×25.2 / 28.6×17.1 —— 长/短 ≈ 1.2~1.7
     * 的**扁眼**，不是横条。
     *
     * ⚠ 2026-09-21 纠正：上一版把这三格改成了 50.4×14.1 的粗横条，理由是
     *   「扫读要扁」。这是**错的，而且是会外溢的错**：
     *   原版真正「横条」的只有 idx 4 / 13 / 22 三个（主轴长/短 = 4.09 / 3.57 / 3.92），
     *   只用于 sleeping / drowsy / bored / sad；而原版的 searching = idx [15,9,3,20,12,18]，
     *   其中 9 / 12 / 18 是**近圆**（长/短 = 1.09 / 1.09 / 1.03）、3 是 1.30 —— 一个横条都没有。
     *   把 scan 族改成横条之后，凡是池子里带 scan 的情绪全部跟着变成「闭眼」：
     *     19 满意     pool ["scan","calm","happy"]        ← scan 在**首位**
     *     39 输出回复  pool ["scan","scan2"]              ← 整池都是 scan
     *     40 检索资料  pool ["scan","scan2","scan3",…]     ← scan 在首位
     *     03 好奇 / 30 思考中                            ← 池尾带 scan，轮换时偶尔中招
     *   实测接触表里 19 / 39 / 40 三格渲染成两条闭眼横条，语义全反。
     *   改回上游原值后这三格恢复「眯眼/环视」，且 19 / 39 不再需要单独覆盖。
     *   → 教训：改一个共享槽位前，先查**谁在用它**（`grep pool` 全表）。 */
  },

  /* 5. 五官 —— 原版全身只有「圆 + 两条白条」，所以这三样全关 */
  features: {
    mouth: false,
    blush: false,
    brows: false
  },

  /* 6. 特效皮肤 —— cloudpuff 的小云泡读作「锅里的热气」，保留（原版也有特效层） */
  fxSkin: 'cloudpuff',

  /* 7. 性格：锅宝是「在岗的伙伴」，不是宠物 —— 待机动作收敛、轮换放慢
   *
   * ⚠ openness（眼睑常驻开合度）必须按**原版的 lid** 重设，不能沿用上游种子值。
   *   上游种子是给「圆眼角色」写的：睡眠 openness 0.08 —— 圆眼压到 8% 是条细缝，
   *   很合理。但我们的「闭眼」是**横条形状**（原版的做法），形状本身已经表示闭合，
   *   再乘 0.08 就变成一根几乎看不见的发丝。
   *   原版自己的数字（replica/src/pose.js 的 lid）：
   *     sleeping  settle 后 lid = 1     ← 完全张开，靠**形状**表示闭眼
   *     drowsy    lid = 0.34
   *   实测佐证：原版 sleeping 截出来是**两条厚横条**，drowsy 是两条细横条，
   *   都不是发丝。所以这里：
   *     00 睡眠  0.08 → 1.0    （原版 sleeping）
   *     15 疲惫  0.55 → 0.34   （原版 drowsy）
   *     06 休眠  0.4  → 0.6    （原版无对应态，取睡眠与困倦之间）
   *   38 拒绝/受限 保持 0.6 —— 原版无对应态，且 0.6 下形状仍然清晰。 */
  emotions: {
    '00': {
      desc: '闭眼成两条横条，右上角 zzz 缓缓飘起 —— 靠形状表示闭眼，不再额外压眼睑',
      openness: 1.0
    },
    '02': {
      poolMs: [11000, 18000],
      anims: [
        { target: 'eyes', prop: 'lookX', type: 'glance', amp: 7, period: 6200 },
        { target: 'eyes', prop: 'lookY', type: 'sine', amp: 2, period: 5400, phase: 0.8 }
      ]
    },
    '06': {
      desc: '半合着眼打盹 —— 比睡眠再合一点，但不至于看不见',
      openness: 0.6
    },
    '15': {
      desc: '眼皮压到三成、身体发沉 —— 撑不住了',
      openness: 0.34
    },
    '16': {
      desc: '眼睛收窄成一条、视线锁住 —— 在干活',
      pool: ['intent'],
      eyes: { both: { open: 0.92 } }
    },
    '30': {
      desc: '视线慢慢扫，偶尔往上飘 —— 在想事情',
      anims: [
        { target: 'eyes', prop: 'lookX', type: 'sine', amp: 9, period: 4200 },
        { target: 'eyes', prop: 'lookY', type: 'sine', amp: 4, period: 3300, phase: 1.4 }
      ]
    },
    '32': {
      desc: '眼睑半合、身体轻微起伏 —— 手上正忙',
      anims: [{ target: 'body', prop: 'y', type: 'sine', amp: 1.6, period: 1700 }]
    },
    '33': {
      desc: '眼睛睁到最大的一条、身体轻弹 —— 交活了',
      pool: ['delight'],
      body: { y: -2, confetti: 0.8 },
      eyes: { both: { y: -3 } }
    },
    '40': {
      desc: '眼睛睁圆了快速左右扫 —— 在翻资料',
      /* 原版 searching = idx [15,9,3,20,12,18]，其中 9 / 12 / 18 / 3 是**近圆**
       * （主轴长/短 = 1.09 / 1.09 / 1.03 / 1.30），一个横条都没有。
       * 我们的 wide / wide2 就是原版的近圆 idx 21 / 3（长/短 1.23 / 1.30）；
       * scan2 是上游三格里最圆的一格（24.8×25.2，长/短 1.02）—— 三格都落在原版
       * searching 的比值区间 1.03~1.30 内。**刻意不带 scan3**（28.6×17.1，长/短 0.60），
       * 它比原版 searching 里任何一个都扁，会在轮换时突然「眯掉」。 */
      pool: ['wide', 'wide2', 'scan2'],
      anims: [{ target: 'eyes', prop: 'lookX', type: 'sine', amp: 11, period: 1500 }]
    }
  }
});
